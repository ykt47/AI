If you want to see the UI

Requirement:
Install Python in Your Vscode
 
1.Open Vscode
2.Open Terminal inside the VsCode
3.key in pip install streamlit in your terminal
4.install this all to ur terminal 
python -m pip install streamlit pandas numpy matplotlib seaborn plotly scikit-learn nltk joblib pillow
5.key in python -m streamlit run app.py to run the UI

Thanks
